<div class="confirm-form-container">
    <h2 class="head-topic">予約確認画面</h2>
    <div class="bg-light w-auto p-5 fs-5">
        <div class="d-flex justify-content-between mb-3">
            <p class="fw-bolder">チェックイン</p>
            <%= l @reservation.check_in, formats: :short %>
        </div>
        <div class="d-flex justify-content-between mb-3">
            <p class="fw-bolder">チェックアウト</p>
            <%= l @reservation.check_out, formats: :short %>
        </div>
        <div class="d-flex justify-content-between mb-3 fw-bolder">
            <p>宿泊人数</p>
            <%= @reservation.population %>人
        </div>
        <div class="d-flex justify-content-between mb-3 fw-bolder">
            <p>宿泊日数</p>
            <%= @stay_days %>日
        </div>
        <div class="d-flex justify-content-between fw-bolder">
            <p>支払い合計金額</p>
            <p>¥<%= @reservation.total_price %></p>
        </div>
        <p class="mt-3">上記内容で問題なければ、『予約を確定』ボタンを押してください</p>
        <div class="d-flex justify-content-end">
            <%= form_with model: @reservation do |f| %>
                <%= f.hidden_field :total_price, value: @reservation.total_price %>
                <%= f.submit "予約を確定", class:"btn btn-outline-primary py-3 px-5 mt-3 me-3 fw-bolder" %>
            <% end %>
                <%= link_to "キャンセル", @room, class:"btn btn-dark py-3 mt-3 px-5 fw-bolder" %>
        </div>
    </div>
</div>